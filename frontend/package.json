{
  "name": "food-order-payment-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node node_modules/vite/bin/vite.js",
    "build": "vite build",
    "preview": "vite preview",
    "android:setup": "npx cap add android",
    "android:sync": "npx cap sync",
    "android:build": "vite build && npx cap sync && cd android && ./gradlew assembleDebug",
    "android:run": "vite build && npx cap sync && npx cap run android",
    "ios:setup": "npx cap add ios",
    "ios:sync": "npx cap sync",
    "ios:open": "npx cap open ios",
    "ios:build": "vite build && npx cap sync && cd ios/App && xcodebuild -workspace App.xcworkspace -scheme App -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' build CODE_SIGNING_ALLOWED=NO ENABLE_USER_SCRIPT_SANDBOXING=NO CLANG_WARN_QUOTED_INCLUDE_IN_FRAMEWORK_HEADER=NO"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.8",
    "@capacitor/cli": "^7.6.8",
    "@capacitor/core": "^7.6.8",
    "@capacitor/ios": "^7.6.8",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "recharts": "^3.10.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.37",
    "@types/react-dom": "^18.2.15",
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
